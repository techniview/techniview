# Runtime mirror of the dependencies in the root pyproject.toml. The backend
# image uses this file; local development and CI use uv.lock.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pymysql==1.2.0
psycopg2-binary==2.9.12
redis==8.1.0
httpx==0.28.1
sqlalchemy==2.0.52
alembic==1.16.5
# dev / test tools (pytest, ruff, pre-commit, pip-audit) are not container
# deps; they come from the `dev` dependency-group in the root pyproject.toml
# via `uv sync --locked`.
