#!/bin/bash
set -euo pipefail

if ! command -v pre-commit >/dev/null 2>&1; then
  echo "pre-commit is not installed. Run 'make install' first." >&2
  exit 1
fi

exec pre-commit run --hook-stage commit "$@"
