#!/bin/bash
set -euo pipefail

if ! command -v uv >/dev/null 2>&1; then
  echo "uv is not installed. Install uv, then run 'make install'." >&2
  exit 1
fi

exec uv run --locked pre-commit run --hook-stage commit "$@"
